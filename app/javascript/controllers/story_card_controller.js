import { Controller } from "@hotwired/stimulus"

// Connects to data-controller="story-card"
export default class extends Controller {
  connect() {
    // Optional: Add any story card specific interactions here
  }
}
